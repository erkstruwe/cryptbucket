describe('encryption module', function () {
	describe('EncryptionService', function () {
		beforeEach(function () {
			module('encryption');
		});

		it('should just work', function () {
		});
	})
});
